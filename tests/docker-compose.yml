version: '3.9'

services:
  tests:
    build:
      context: ./
    container_name: tests
    volumes:
      - ./:/usr/src/tests   
    networks:
      - trans_network

networks:
  trans_network:
    external: true